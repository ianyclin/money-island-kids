# 交班 2026-09-07：小小理財島 PWA 重寫第一版

## 狀態
- 分支 `pwa`，全部 commit 完（HEAD fedd8d5）。成品在 `docs/`，尚未推上 GitHub、尚未開 Pages。
- 瀏覽器（手機尺寸）跑過：建立帳本 → 記零用錢（35/65 拆分）→ 花費 → 自主多存 → 家長核准 → 調比例 → 新增專案 → 記買入 → 手動市值 → 過年收成 → 夢想罐（active／queued）→ 每月回顧 → 所有紀錄編輯 → 匯出／匯入（標準＋可攜＋帶照片）→ 重開 app 自動發每月獎勵、自動抓證交所收盤價（0050 真實價 109.9）。
- 引擎自我測試 `docs/dev/selftest.html`：PASS 119 / FAIL 0。
- 裁判（fable xhigh）逐支比對 store.js 對 money-store.ts：金額規則零差異，14 條發現已全部修掉（第 5 條「刪掉獎勵後同月可能再發」是原站還原備份後也會有的行為，保留）。

## 沒做／沒驗
- iOS 模擬器需要完整 Xcode，這台沒有；只用 Chrome 手機模擬驗過。真機 Safari 的「加入主畫面」與離線啟動要作者自己試。
- Gist 真實連線沒測（需要作者的金鑰）。
- 圖示是 PIL 畫的暫用版；作者若同意，從舊站抓 4 張原圖放進 `docs/icons/` 蓋掉即可（檔名一樣）。
- 舊站真實的可攜備份檔沒有樣本，用同格式的模擬檔驗過。

## 作者要做的
1. GitHub Desktop：把 `pwa` 推上去、合併進 `main`。
2. GitHub 倉庫 Settings → Pages → Deploy from a branch → `main` + `/docs`。
3. 舊站家長區「下載完整可攜備份」→ 新網址第一次使用選「從備份檔還原」。
4. Safari 打開新網址 → 分享 → 加入主畫面。

## 改程式的鐵律
- 改了 `docs/` 任何檔案，`docs/sw.js` 的 `VERSION` 加一號；新增 js 檔要加進 `CORE`。
- 業務規則以 `db/money-store.ts` 為準，錯誤文案一字不改。
- 本機預覽：上層 `.claude/launch.json` 的「小小理財島 PWA」（8792）。沒有 Node；`jsc --module-file=` 可以跑 ES module。

---

# 交班補記（同日稍晚）：手機優先重新設計

## 狀態
- 分支 `pwa-mobile`（從 `pwa` 分出），HEAD 是本次 commit。`pwa` 分支保留舊版畫面可回退。
- 設計流程：17 個代理的設計工作流（理念抽取、四頁稽核、四份提案、三位裁判、綜合、兩位批評者）→ `notes/mobile-redesign/spec.md`（第 9 節是主線拍板，優先於前文）→ 18 個代理的建造工作流（共用層、四頁、逐頁審查與修正、整合審查）→ 主線修正共通缺陷。
- 主線修正的共通缺陷：`app.js` 每次重繪前呼叫 `closeModal("rerender")`，各頁 onClose 看到 "rerender" 只清「DOM 開著」旗標、不清「該開」旗標，mount 再補開並回填。已在瀏覽器驗證（打到一半的操作碼重繪後還在）。
- 真實備份資料（作者 2026-09-07 匯出）在瀏覽器面板的 localStorage 裡驗過四頁；docs/dev 的副本已刪。

## 量測（375×812，作者真實資料）
| 頁 | 改版前 | 改版後 |
|---|---|---|
| 孩子首頁 | 5.7 屏 | 3.3 屏（三格錢包在第一屏） |
| 夢想與回顧 | 3.9 屏 | 1.6 屏 |
| 所有紀錄（16 筆） | 11.2 屏 | 3 屏 |
| 家長區（解鎖） | 3.8 屏（收合）| 2.4 屏 |

## 沒做／待作者看過再決定
- 圖示仍是暫用版。
- 投資子頁單一持股時「選擇賣出標的」仍是下拉，沒有改成純文字（規格 2.4.1 的小簡化）。
- 家長區未解鎖時「資料與備份」面板仍顯示（按鈕灰掉）；想更短可以隱藏。
- 真機 Safari 的貼底列、鍵盤、standalone 模式（規格 3.7 清單）沒有真機驗證。
